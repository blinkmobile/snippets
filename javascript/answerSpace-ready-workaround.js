/**
 * Available since 2012W35.
 * 
 * This JavaScript could be inserted in a script tag in the htmlHead setting.
 * Our 'viewShow' event is triggered after every view transition.
 * Using jQuery's one() method has this code execute exactly once (just the first 'viewShow')..
 */

$(document).one('viewShow', function() {
  // startup progress-dot screen is gone now
  // TODO: fill this out with the code you want to run
});
